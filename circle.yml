  dependencies:
    override:
      - lein install

  test:
    override:
      - lein new magic my-project-name my-project-id my-gke-cluster-name

  deployment:
    prod:
      branch: master
      commands:
        - lein deploy clojars
