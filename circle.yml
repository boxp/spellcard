machine:
  environment:
    CLOJARS_USERNAME: ${CLOJARS_USERNAME}
    CLOJARS_PASSWORD: ${CLOJARS_PASSWORD}

    test:
      override:
        - lein install
        - lein new magic my-project-id my-gke-cluster-name

      deployment:
        prod:
          branch: master
          commands:
            - lein deploy clojars
